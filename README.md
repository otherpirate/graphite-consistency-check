# Get Started
```
git clone https://github.com/otherpirate/graphite-consistency-check.git

cd graphite-consistency-check

pip install -r requirements.txt

python main.py -u http://graphite.YOUR.DOMAIN/ -d 10 -f 2hours -t 10minutes -g 1minute -s avg -e YOUR.ENVIROMENT.FOR.INSTANCES -i INSTANCE_01 INSTANCE_02 -m men.men_free men.men_used
```
